---
import "../styles/global.css";
import "../styles/components.css";



import modalScriptUrl from "../scripts/modal.js?url";

import ResumeModal from "../components/ResumeModal.astro";

const { title = "Portfolio", description = "Lorem ipsum portfolio." } = Astro.props;
---

<!doctype html>
<html lang="es" data-theme="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>

    <script>
      (function () {
        try {
          const savedTheme = localStorage.getItem("theme");
          const systemPrefersDark =
            window.matchMedia && window.matchMedia("(prefers-color-scheme: dark)").matches;

          const resolvedTheme = savedTheme ? savedTheme : (systemPrefersDark ? "dark" : "light");
          document.documentElement.setAttribute("data-theme", resolvedTheme);
        } catch (err) {}
      })();
    </script>
  </head>

  <body>
    <slot />

    <!-- ✅ Modal global (una sola vez) -->
    <ResumeModal />

    <!-- ✅ Scripts globales -->
    <script type="module" src="/src/scripts/theme.js"></script>
  <script type="module" src="/src/scripts/modal.js"></script>
  </body>
</html>